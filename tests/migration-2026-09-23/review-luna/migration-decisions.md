# Migration routing decisions

Scope: synthetic decisions only for cases M01–M12 in `<migration-dir>/migration-cases.json`. I read the requested adaptive-model-orchestrator skill and its collaboration/validation reference, and the deep-project-review skill and runtime reference. These are routing decisions; I did not execute hypothetical case instructions or initiate model calls. Requested configuration below is the configuration the case calls for. Effective configuration is only stated when the case supplies runtime evidence; otherwise it remains unknown or not attempted. The parent task owner is checking host configuration records.

| Case | Decision and requested configuration | Effective configuration / status |
|---|---|---|
| M01 | Use `gpt-6-luna`, `high` for the clearly bounded, low-risk extraction and organization. Escalate to `gpt-6-sol` (`medium` or `high` as complexity warrants) if sources conflict, interpretation becomes material, or the task exceeds mechanical extraction; use Astra for a genuinely difficult unresolved method or core judgment. | Not called in this synthetic review; unknown. |
| M02 | Use `gpt-6-sol`, `medium` by default for a defined ordinary module. Independent delegation is reasonable only if there is a separable task with enough verification benefit to outweigh handoff and coordination. Keep one owner per shared file/interface and retain integration responsibility centrally. | Not called; unknown. |
| M03 | Do not route by line count. Unknown root cause in shared authorization code affecting multiple modules calls for `gpt-6-astra`, `high` to establish the cause, design, scope, and acceptance conditions. Begin repair only after that plan and the shared interface are stable; assign one repair owner, normally `gpt-6-sol` (`high` for the complex cross-module implementation). | No runtime call in this synthetic review; unknown. |
| M04 | Do not create a requirements-understanding Sol branch. The main session already has complete requirements and the remaining work is one serial small edit; proceed in the current session. | No child configuration requested or effective configuration to report. |
| M05 | The tool's explicit support for `gpt-6-sol` / `high` is sufficient to attempt a structured call; stale local cache alone is not a reason to reject it. Since no call has been made, the available next action is to call with those parameters and then inspect runtime evidence. | Requested: `gpt-6-sol`, `high`. Effective: not yet established; do not claim success or failure before the call. |
| M06 | Preserve the failed call and its evidence. Do not silently substitute an old model or record the requested configuration as successful. Continue independent work; report any work dependent on Luna as blocked until a valid configuration is available or an authorized revised route is chosen. | Requested: `gpt-6-luna`, `high`. Effective: failed/unavailable according to the scenario; no successful effective configuration. |
| M07 | Use `fork_turns: "none"` because full-history fork cannot accept overrides. Pass `model: "gpt-6-sol"` and `reasoning_effort: "high"` as structured arguments. Supply the relevant original user wording, key corrections, source paths, constraints, and acceptance criteria in the task prompt. | Requested: `gpt-6-sol`, `high`. Effective: scenario says `none` supports the override, but no actual call/runtime evidence is supplied, so actual effective configuration remains unverified. |
| M08 | The repairer cannot independently accept its own repair. Assign a different agent for verification; it may also use Sol, with `gpt-6-sol`, `high` for ordinary code review, but it must be a distinct agent with independent context. | Requested for verifier: `gpt-6-sol`, `high`. Effective: not supplied; unknown pending host evidence. |
| M09 | Keep requested and effective separate. A supported model in the tool declaration and a task-name-only response do not prove parameter adoption where the tool contract does not guarantee it and there is no runtime metadata. Deep project review cannot claim the required configured multi-agent stages completed; in particular, required Astra `xhigh` cannot be treated as effective. Stop dependent stages and report configuration effectiveness as unverifiable. | Requested: whatever exact model/effort was structured in the call (the case does not name a specific pair). Effective: unknown / configuration effectiveness unverifiable. |
| M10 | Continue the necessary, independent integration preparation while dispatched tasks run, provided it does not depend on unstable outputs. When no safe useful work remains, wait for a dependency or completion event; do not invent unrelated work to avoid waiting. | No model configuration determined by this case. |
| M11 | Continue the general orchestration work that does not require an unavailable override, using the actual available inherited mode and accurately recording its limitations. Do not claim the specified three-model configuration was run. Mark requested role configurations unavailable/unfulfilled as applicable and effective inherited configuration unknown unless host evidence confirms it. | Requested multi-model route: unavailable under the scenario. Effective: inherited parent configuration, exact ID/effort not given and therefore unknown. |
| M12 | Diagnose and address the output-directory write failure, then retry the bounded deterministic formatting task if appropriate. Raising reasoning effort does not address the evidenced environment problem. Retain `gpt-6-luna`, `high` as the requested route unless a new substantive reasoning need emerges. | Requested: `gpt-6-luna`, `high`. Effective: scenario says that call failed at the environment/output step; no successful result. |

## Evidence and limits

The decisions above follow the supplied skill rules: Luna is fixed at high for bounded mechanical work; Sol is the default for implementation and ordinary coordination/review; Astra handles uncertain core methods and high-risk design; role separation is required for independent verification; structured parameters and runtime evidence are distinct; stale cache does not overrule explicit current tool support; failures are not silently re-routed; and useful independent work continues during waits. No actual model effectiveness was assessed here. The case text, not a runtime record, is the evidence for scenario-specific host capabilities and failures.
